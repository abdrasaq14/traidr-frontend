import styled from "styled-components";

export const MiniAddToWishListContainer = styled.div `
    cursor: pointer;
    position: relative;
    .add-to-wishlist-icon{
        color: var(--orange-color);
    }

`