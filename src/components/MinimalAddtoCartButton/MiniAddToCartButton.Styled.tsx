import styled from "styled-components";

export const MiniAddToCartContainer = styled.div`
    cursor: pointer;
    position: relative;

    .add-to-cart-icon{
        color: var(--orange-color);
    }
`